apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: gateway-server
  name: gateway-server
  namespace: my-sso
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gateway-server
  strategy: {}
  template:
    metadata:
      labels:
        app: gateway-server
      # annotations:
      #   co.elastic.logs/enabled: "true"
      #   co.elastic.logs.json-logging/json.keys_under_root: "true"
      #   co.elastic.logs.json-logging/json.add_error_key: "true"
      #   co.elastic.logs.json-logging/json.message_key: "message"
    spec:
      containers:
      - image: forsrc/my-sso:gateway-server
        name: gateway-server
        ports:
        - containerPort: 8080
        imagePullPolicy: IfNotPresent #Always
        resources: {}
        env:
        - name: MY_PROFILE
          value: k8s
        - name: MY_OAUTH2_SERVER
          value: http://oauth2-server
        - name: MY_RESOURCE_SERVER
          value: http://:resource-server
        - name: MY_CLIENT_SERVER
          value: https://client-server      ############### https
        - name: MY_GATEWAY
          value: https://gateway-server     ############### https
        - name: MY_EUREKA_SERVER
          value: http://forsrc:forsrc@eureka-server/eureka
        - name: MY_ADMIN_SERVER
          value: http://admin-server
        livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
        initContainers:
        - name: init-waiting
          image: busybox:1.28
          command: ['sh', '-c', 'until nc -z eureka-server 80; do echo waiting for eureka-server 80; sleep 2; done;']

---

apiVersion: v1
kind: Service
metadata:
  name: gateway-server
  namespace: my-sso
spec:
  selector:
    app: gateway-server
  ports:
  - port: 80
    targetPort: 8080
    name: gateway-server
  type: ClusterIP

